---
title: "Diagnosing Failure Modes in AlphaZero-Style Self-Play Reinforcement Learning (chess testbed)"
description: "A from-scratch AlphaZero-style learner (network plus tree search), used to find and document how self-play training fails: a value-head zero attractor, a repetition exploit, and soft draw drift."
categories: ["Simulation & ML Engineering", "Python", "PyTorch", "python-chess", "Monte Carlo Tree Search (PUCT)"]
order: 100
featured: false
draft: true
---

[Code](https://github.com/lonespear/archon){.btn .btn-outline-primary .btn-sm}

## Results

- Diagnosed a value-head zero attractor: with draws scored 0, the value loss collapsed to 0.0000 and self-play went 0W/8D/0L. Hyperparameter-only fixes (more noise, rolling back to earlier checkpoints) failed. A non-zero draw target restored a non-zero value loss (about 0.02 to 0.08).
- Found a specification-gaming exploit: the agent learned threefold-repetition draws in about 8 moves (iteration time fell from 3-5 min to 30-60 s). Closed by disabling automatic draw claims.
- Showed that raising root exploration noise at 25 simulations raised decisive games (4% to 15%) only through noise-driven blunders, which reverted when noise was removed.
- Network sizes: Archon 8,874,951 parameters; Marchon MoE variant 11,990,343 parameters.

## The problem

Self-play learners optimise the reward they are given, not the behaviour intended. With limited compute, training can settle into degenerate equilibria (all draws, instant repetition) while loss curves still look healthy.

## What I built

Dual-head residual network (8.87M parameters) guided by PUCT Monte Carlo Tree Search, trained only on self-play outcomes. The run was instrumented (policy/value loss, rolling decisive-game rate, eval W/D/L). Each pathology got a hypothesis, an intervention and a logged outcome across about 3,500 iterations. A variant (Marchon) replaces the last residual blocks with a Mixture-of-Experts tower and adds an optional supervised warm start; it is not yet trained.

::: {.callout-note appearance="simple"}
## What it is for

Any model trained or tuned against itself, such as red-vs-blue adjudication or self-play COA exploration, can converge to specification-gaming behaviour and can look like it is improving on an internal metric. The project shows how to spot these failures from instrumentation, and why a self-referential rating on deterministic evaluations measures nothing.
:::

## Tools

Python, PyTorch, python-chess, Monte Carlo Tree Search (PUCT), residual CNN, Mixture-of-Experts, Flask/SSE dashboard
